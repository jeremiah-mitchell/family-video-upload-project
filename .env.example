# Family Video Tagger - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Jellyfin server URL
# The full URL to your Jellyfin server, including protocol and port
# Example: http://jellyfin:8096 (Docker) or http://192.168.1.100:8096 (local network)
JELLYFIN_URL=http://jellyfin:8096

# Jellyfin API key
# Generate this in Jellyfin: Dashboard > API Keys > Add
# Keep this secret - never commit real values to git!
JELLYFIN_API_KEY=your-jellyfin-api-key-here

# Host media path (for Docker volume mount)
# This is the path ON THE HOST where your videos are stored
# For UGREEN NAS: /volume3/vault/home-videos
# For Synology: /volume1/home-videos
HOST_MEDIA_PATH=/volume3/vault/home-videos

# =============================================================================
# OPTIONAL VARIABLES (with defaults)
# =============================================================================

# Jellyfin username for Now Playing feature
# This user's currently playing video will show in the app
JELLYFIN_USER=jeremiah

# API server port (default: 3001)
# PORT=3001

# Web server port (default: 3000)
# WEB_PORT=3000

# API port on host (default: 3001)
# API_PORT=3001

# CORS origin for frontend (default: http://localhost:3000)
# In production, set this to your actual frontend URL
# CORS_ORIGIN=http://localhost:3000

# Public API URL for frontend (browser-side requests)
# Set this to the URL where the API is accessible from the browser
# NEXT_PUBLIC_API_URL=http://nas-ip:3001
